{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Multiagents Chat: Extension Host",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/extensions/multiagents-chat",
        "${workspaceFolder}"
      ]
    }
  ]
}